import clsx from "clsx";
import { MapPinIcon, PhoneOutgoingIcon, MailIcon, SearchIcon } from "lucide-react";

export default function Header() {
  return (
    <header>
      <h1 className="py-8 text-center text-6xl font-bold">Aqui é a HEADER</h1>
    </header>
  );
}
